import { Routes } from '@angular/router';

import { BoletosPage } from './features/boletos/boletos.page';

export const routes: Routes = [
  { path: '', component: BoletosPage },
  { path: 'index.html', component: BoletosPage },
];
